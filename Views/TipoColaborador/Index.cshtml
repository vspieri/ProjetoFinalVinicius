@model IEnumerable<ProjetoFinalVinicius.Models.TipoColaborador>

@{
    ViewData["Title"] = "Index";
}

<h1>Tipo Colaborador</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoColaboradorNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoColaboradorNome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoColaboradorId">Edita</a> |
                <a asp-action="Details" asp-route-id="@item.TipoColaboradorId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoColaboradorId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
